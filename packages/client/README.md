# @yifun/qipai-cli

终端棋牌大厅客户端。全局命令：`yifunqipai`。

配置：`~/.yifunqipai/config.json`

```bash
npm i -g @yifun/qipai-cli
yifunqipai config set serverUrl ws://your-host:8787
yifunqipai
```

详见仓库根目录 README。
